PORT=4000

# Optional. Without it the server falls back to an in-memory store.
# mongodb+srv://<user>:<password>@<cluster>.mongodb.net/ask_the_lecture?retryWrites=true&w=majority
MONGODB_URI=

ANTHROPIC_API_KEY=
